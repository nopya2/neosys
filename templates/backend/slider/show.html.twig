{% extends 'base.html.twig' %}

{% block title %}Slider{% endblock %}

{% block body %}
    <h1>Slider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slider.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ slider.sousTitre }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ slider.imageName }}</td>
            </tr>
            <tr>
                <th>ImageSize</th>
                <td>{{ slider.imageSize }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slider_index') }}">back to list</a>

    <a href="{{ path('slider_edit', {'id': slider.id}) }}">edit</a>

    {{ include('slider/_delete_form.html.twig') }}
{% endblock %}
